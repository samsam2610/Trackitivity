# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'M2M' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod 'ResearchKit', '~> 1.0'
  # Pods for M2M

  target 'M2MTests' do
    inherit! :search_paths

    # Pods for testing
  end

  target 'M2MUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
